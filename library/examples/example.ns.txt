:: lang_version: neuroscript@0.5.0
:: file_version: 1.0

func Multiply(needs a, b returns result) means
  :: description: Multiply two integers digit-by-digit.
  :: param:a: First number.
  :: param:b: Second number.
  :: return:result: Product of a and b.
  :: algorithm: 1. Initialize result as "0". 2. For each digit in b, compute partial products.
  :: caveats:  Currently only returns "0".  Digit-by-digit multiplication not yet implemented.
  set result = 0
  return result
endfunc

