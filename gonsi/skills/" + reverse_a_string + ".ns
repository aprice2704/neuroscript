ReverseString.ns
DEFINE PROCEDURE ReverseString(string)
COMMENT This procedure reverses a given string.
  stringLength = LENGTH(string)
  reversedString = ""
  FOR i = stringLength TO 1 STEP -1
    reversedString = reversedString + SUBSTRING(string, i, 1)
  ENDFOR
  RETURN reversedString
END