DEFINE PROCEDURE ReverseString(inputString)
COMMENT:
PURPOSE: Reverses a given input string.
INPUTS: inputString - The string to be reversed.
OUTPUT: reversedString - The reversed string.
ALGORITHM: Iterates through the input string from the end to the beginning and concatenates each character to build the reversed string.
END COMMENT
SET reversedString = ""
FOR EACH char IN inputString DO
  SET reversedString = char + reversedString
END
END